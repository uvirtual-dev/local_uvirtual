on:
  push:
    branches:
      - campus_antiguo
name: 🚀 Deploy campus_antiguo branch to https://usal.uvirtual.org/campus
jobs:
  web-deploy:
    name: 🎉 Deploy to campus
    runs-on: ubuntu-latest
    steps:
      - name: 🚚 Get latest code
        uses: actions/checkout@v3

      - name: 📂 Sync files
        uses: SamKirkland/FTP-Deploy-Action@4.3.3
        with:
          server: ${{ secrets.SERVER_PROD_ANTIGUO}}
          username: ${{ secrets.USER_PROD_ANTIGUO}}
          password: ${{ secrets.PASS_PROD_ANTIGUO}}
          server-dir: /public_html/campus/local/uvirtual/
